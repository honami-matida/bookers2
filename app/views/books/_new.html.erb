<table class='table'>
  <h2>User info</h2>
        <tr>
          <%= image_tag 'default-image.jpg' %> <!-- 固定値 -->
        </tr>
        <tr>
          <th>name</th>
          <th>DMM花子</th> <!-- 固定値 -->
        </tr>
        <tr>
          <th>introduction</th>
          <th></th> <!-- 固定値 -->
        </tr>
</table>

<div class='row'>
	      <!-- 編集リンクが未実装 -->
</div>

<%= form_with model: book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>