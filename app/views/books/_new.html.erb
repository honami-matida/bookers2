<table class='table'>
  <h2>User info</h2>
        <tr>
          <%= image_tag 'default-image.jpg' %> <!-- 固定値 -->
        </tr>
        <tr>
          <th>name</th>
          <th><%#= @user.name %></th> <!-- 固定値 -->
        </tr>
        <tr>
          <th>introduction</th>
          <th><%#= @user.introduction %></th> <!-- 固定値 -->
        </tr>
</table>

<div class='row'>
  <%#= link_to "プロフィール編集", edit_user_path(@user.id) %></p>
</div>

<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>